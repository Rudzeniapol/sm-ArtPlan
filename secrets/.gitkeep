This directory is mounted into the bot container at /secrets (read-only).
Put your Google service-account JSON here as google-credentials.json
if you want /sync to work. Otherwise this directory can stay empty.
